#include<iostream>

using namespace std;

int *growArray(int* p_values, int cur_size)

int main ()
{
	int next_element = 0;
	int size = 10;
	int *p_values = new int[size];
	int val;
	cout<<"Please enter a number: ";
	cin>>val;
	while(val > 0)
	{
		if(size == next_)

